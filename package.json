{
  "name": "jdlauncher",
  "version": "1.0.0",
  "private": true,
  "description": "Static launcher page for JD's projects.",
  "scripts": {
    "build": "node scripts/build.js",
    "publish": "npm run build && git add . && git commit -m \"Update launcher\" && git push origin main"
  }
}
